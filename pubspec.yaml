name: ansi_modifier

description: Style and animate terminal output by adding,
             replacing, modifying, and clearing ANSI escape codes.

version: 1.1.0

homepage: https://github.com/simphotonics/ansi_modifier

topics:
  - ansi
  - escape-code
  - console
  - color
  - cursor-position

environment:
  sdk: '^3.13.0'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
